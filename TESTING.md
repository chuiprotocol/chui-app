# TESTING.md —— 測試策略

## 自動化（CI 每次 push 都跑）

| 層 | 指令 | 涵蓋 |
|---|---|---|
| 重排序引擎 | `pnpm --filter @chui/hub-worker test` | 9 個關鍵行為＋55 筆評估集門檻（訂單正確率 ≥90%、應澄清 0 無聲錯）＋奶茶店真機回歸 |
| 意圖判讀 | 同上（intents 測試） | 確認/取消/離場、同音誤辨、簡→繁、回音過濾、「不要辣≠取消」等 32 案例 |
| 型別與建置 | CI 全 job | Worker＋四前端 tsc/build |
| Move 合約 | chui-contracts `sui move test` | vault 限額/撤銷/領回、log_policy 取鑰授權，17 案例 |

## 端到端（wrangler dev 本地可跑）

parse（多品項/客製化/澄清 422）→ confirm（發號流水）→ settlement
（假 digest 誠實 pending）→ 店家看板明細 → 開店註冊（真簽名/壞簽名 401）。

## 真機驗證清單（需實體手機＋Slush）

- [ ] 語音全流程：授權→點餐→覆誦→「確認下單」→倒數→扣款→單號
- [ ] 指令詞：「取消下單」「結束對話」「鹽酥雞不要辣」
- [ ] 插話：Agent 覆誦到一半開口打斷
- [ ] 存證：付款後 Seal 卡出現、用同錢包解密成功、換錢包解密失敗
- [ ] 店家：錢包登入看板、即時跳單、解密對話
- [ ] 開店：新錢包簽名開店→對新店點餐→新店看板收單
- [ ] 離場：「⏏ 一鍵領回全部」後 Slush 餘額回來、Agent 動不了錢
