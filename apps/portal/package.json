{
  "name": "@chui/portal",
  "private": true,
  "version": "0.1.0",
  "description": "嘴付公版商家入口：消費者選店（如好喝奶茶店）→ 純語音零按鍵下單結帳",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "npm run lint",
    "dev": "vite --port 9400"
  },
  "dependencies": {
    "@chui/web": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
