// 嘴付公版入口——Cloudflare Worker（純靜態資產）部署設定
// Workers Builds 的 Deploy command 用：npx wrangler deploy --config apps/portal/wrangler.jsonc
{
  "name": "chui-portal",
  "compatibility_date": "2025-01-01",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
